---
- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: Install a list of packages
    ansible.builtin.apt:
      pkg:
      - python3-pip
      - openssh-server
      - scdaemon
      - paperkey
      - stow
      - zsh
      - fzf
      - zoxide
      - exa
      - tmux
      - tree
      - pass
      - pwgen
      - sqlite3
      - libssl-dev
      - postgresql-13
      - ripgrep
      - rsync
      - librsync-dev
      - ncdu
      - ufw
      - nmap
      - libpam-yubico
      - libyubikey-dev
      - yubikey-personalization
      - yubikey-luks
      - wireshark
      - ninja-build
      - gettext
      - cmake
      - apt-file
      - xbindkeys
      - whois
      - dnsutils
      - libpam-yubico
      - libyubikey-dev
      - yubikey-personalization
      - yubikey-luks
      - yubikey-manager
      - libpam-u2f
      - pcscd
      - libbz2-dev
      - libncurses-dev
      - libffi-dev
      - libreadline-dev
      - libsqlite3-dev
      - libzma-dev
      - tk-dev
      - libasound2-dev
      - libudev-dev
